@model IEnumerable<E_PRESCRIBING_SYSTEM.Models.Ward>

@if (Model != null && Model.Any())
{
    @foreach (var med in Model)
    {
        <tr>
            <td>@med.WardID</td>
            <td>@med.WardName</td>
            <td>@med.Bed</td>
            <td>
                <a asp-controller="Pharmacist" asp-action="Update" asp-route-id="@med.WardID">Update</a>
                <a asp-controller="Pharmacist" asp-action="Delete" asp-route-id="@med.WardID">Delete</a>
            </td>
        </tr>
    }
}
else
{
    <tr>
        <td colspan="3">No results found.</td>
    </tr>
}
