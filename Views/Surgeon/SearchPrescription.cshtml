@* @model IEnumerable<E_PRESCRIBING_SYSTEM.Models.NewPrescription>

@{
    ViewBag.Title = "Search Prescriptions";
    Layout = "~/Views/Shared/_SurgeonLayout.cshtml";
}

<h2>Search Prescription Results</h2>


<p>
    <a asp-action="PrescriptionListSurgeon" asp-controller="Surgeon" class="btn btn-primary">Prescription list</a>
</p>

<!-- Display Search Results -->
@if (Model != null && Model.Any())
{
   
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Prescription ID</th>
                <th>Patient ID</th>
                <th>Patient Name</th>
                <th>Medication</th>
                <th>Quantity</th>
                <th>Date</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var prescription in Model)
            {
                <tr>
                    <td>@prescription.PrescriptionId</td>
                    <td>@prescription.PatientProfile.PatientIDno</td>
                    <td>@prescription.PatientProfile.PatientName @prescription.PatientProfile.PSurname</td>
                    <td>@prescription.PharmacyMedication.Name</td>
                    <td>@prescription.Quantity</td>
                    <td>@prescription.DateAdded.ToString("yyyy-MM-dd")</td>
                    <td>@prescription.Status</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No prescriptions found for the provided search criteria.</p>
}
 *@